<Window x:Class="TextForge.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="TextForge Studio"
        Icon="TextForge.ico"
        Height="800" Width="1280"
        MinHeight="600" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="#1f2127">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      CornerRadius="0"
                      GlassFrameThickness="0"
                      ResizeBorderThickness="6" />
    </WindowChrome.WindowChrome>
    <Grid>
        <wv2:WebView2 x:Name="WebView"
                      Visibility="Collapsed"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch" />

        <!-- Loading overlay shown first while WebView initializes -->
        <Grid x:Name="LoadingOverlay" Visibility="Visible" Background="#1f2127">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Orientation="Vertical">
                <Image Source="TextForgeIconLarge.png"
                       Width="64" Height="64"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       Margin="0,0,0,16" />
                <TextBlock x:Name="QuoteText"
                           Foreground="#8b949e"
                           FontSize="14"
                           FontFamily="Segoe UI"
                           FontStyle="Italic"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="420" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
